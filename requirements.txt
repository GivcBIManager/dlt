# Oracle 11g -> Iceberg dlt pipeline
dlt[filesystem]>=1.25.0
pyiceberg[sql-sqlite]>=0.11.0   # [sql-sqlite] pulls SQLAlchemy, required by the
                                # in-memory SQLite Iceberg catalog dlt's filesystem
                                # destination uses at load time
oracledb>=2.0.0          # thick mode required for Oracle 11g (thin needs 12.1+)
pyarrow>=14.0.0
