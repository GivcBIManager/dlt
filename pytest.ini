[pytest]
# Use a repo-local basetemp: the user's system temp holds a stale, ACL-locked
# pytest-of-<user> dir that breaks pytest's default tmp_path scan on Windows.
addopts = --basetemp=.pytest_tmp
testpaths = tests
